import React from 'react';

// import { Pane } from '@nx-ie11-test/ui-core';
import { LoginForm } from '../components';

export const Login: React.FC = () => {
    return (
        <React.Fragment>
            <LoginForm />
        </React.Fragment>
    );
};

export default Login;
